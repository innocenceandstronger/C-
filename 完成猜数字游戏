#include <stdio.h>
int main()
{
	int number,a;
	a=36;
	while(1)
	{
		scanf("%d",&number);
		if (number>a)
		{
			printf("大了\n");
		}
		else if(number<a)
		{
			printf("小了\n");
		}
		else if (number==a)
		{
			printf("对了\n");
			break;
		}
	}
	return 0;
}
