#include<stdio.h>
#include<Windows.h>
void result()
{
	int x;
	for (x = 1; x <= 4; x++)
	{
		if ((x != 1) + (x == 3) + (x == 4) + (x != 4) == 3){
			printf("%d is murderer!\n",x);
		}
	}
}
		
int main()
{
	result();
	system("pause");
	return 0;
}
