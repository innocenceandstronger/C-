#include<stdio.h>
int main()
{
	int x,y,z,sum;
	for(x=0;x<=33;x=x+1)
	{
		for(y=50;y>=0;y=y-1)
		{
			z=100-x-y;
			if(z%2!=0) continue;
			sum=3*x+2*y+z/2;
			if(sum==100)
			printf("%d,%d,%d\n",x,y,z);
		}
	}
	return 0;
}

